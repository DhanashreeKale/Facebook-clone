{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "password",
  "database": "facebook",
  "synchronize": false,
  "logging": false,
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["src/seed-migration/**/*.ts"],

  "cli": {
    "entitiesDir": "src/modules/**/*",
    "migrationsDir": "src/seed-migration"
  }
}
